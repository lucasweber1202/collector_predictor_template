# Required — corporate deployment surface (do not remove)
azure-identity>=1.12
azure-keyvault-secrets>=4.4
databricks-sqlalchemy>=0.2.8
psycopg2-binary>=2.9
pyspark==4.1.1

# Common across collectors — keep unless you have a reason not to
httpx>=0.27
sqlalchemy>=2.0
pandas>=2.0

# Source-specific parsers — keep only what the source actually needs
